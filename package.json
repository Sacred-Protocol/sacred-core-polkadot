{
  "name": "@sacred/contracts",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network polkadotHubTestnet",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "@parity/hardhat-polkadot": "^0.1.10",
    "hardhat": "2.26.0",
    "solc": "0.8.28"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
