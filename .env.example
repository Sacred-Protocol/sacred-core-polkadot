# Deployment Environment Variables (example)
# 1) Copy this file to ".env" in the same directory
# 2) Fill in your own values
# 3) .env is gitignored and should never be committed

# EOA used for deployments (hex string with 0x prefix)
DEPLOYER_PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# Address that signs claim attestations
ATTESTER_ADDRESS=0xYourAttesterAddress

# Address that receives protocol fees
FEE_RECIPIENT_ADDRESS=0xYourFeeRecipientAddress

# Protocol fee in basis points (e.g. 100 = 1%)
FEE_BPS=100

# RPC endpoint for the target network
RPC_URL=https://testnet-passet-hub-eth-rpc.polkadot.io
